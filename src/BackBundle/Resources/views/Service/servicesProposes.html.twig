{% extends "back.html.twig" %}
{% block content %}
    <main class="content">
        <div class="container-fluid p-0">

            <h1 class="h3 mb-3">Liste des Proposition</h1>

            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-body">
                            <table id="datatables-basic" class="table table-striped" style="width:100%">
                                <thead>
                                <tr>
                                    <th>Nom</th>
                                    <th>Categorie</th>
                                    <th>Description</th>
                                    <th>Actions</th>


                                </tr>
                                </thead>
                                <tbody>
                                {% for s in serviceProposes %}
                                    <tr>
                                        <td>{{ s.nomService }}</td>
                                        <td>{{ s.categorieService}}</td>
                                        <td>{{ s.description}}</td>
                                        <td><a href="{{ path('back_etudeProposition',{'id': s.id}) }}"><button class="btn btn-warning"> Accepter</button></a>
                                            <a href="{{ path('back_supprimerService',{'id': s.id}) }}"><button class="btn btn-danger">Rejeter</button></a> </td>

                                    </tr>
                                {% endfor %}

                                </tbody>
                                <tfoot>
                                <tr>
                                    <th>Id</th>
                                    <th>Nom</th>
                                    <th>Description</th>

                                </tr>
                                </tfoot>
                            </table>
                        </div>
                    </div>
                </div>


            </div>

            <script>
                document.addEventListener("DOMContentLoaded", function(event) {
                    // Datatables basic
                    $('#datatables-basic').DataTable({
                        responsive: true
                    });
                    // Datatables with Buttons
                    var datatablesButtons = $('#datatables-buttons').DataTable({
                        lengthChange: !1,
                        buttons: ["copy", "print"],
                        responsive: true
                    });
                    datatablesButtons.buttons().container().appendTo("#datatables-buttons_wrapper .col-md-6:eq(0)")
                });
            </script>

        </div>
    </main>
{% endblock %}